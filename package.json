{
  "name": "emoji-react",
  "version": "0.0.1",
  "description": "Dead simple emoji react component",
  "main": "lib/index.js",
  "repository": {
    "url": "https://github.com/gkaemmer/emoji-react",
    "type": "git"
  },
  "scripts": {
    "build": "npm run build:clean && npm run build:lib",
    "build:clean": "rm -rf lib",
    "build:lib": "babel src --out-dir lib",
    "prepublish": "NODE_ENV=production npm run build"
  },
  "author": "Graham Kaemmer",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "babel-preset-stage-2": "^6.18.0"
  },
  "peerDependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1"
  }
}
